# Local samples

This directory is intentionally empty in the public repository. Place licensed
input frames, videos, and auxiliary mocap assets here or point the benchmark
scripts to an external data root. The DFA Panda protocol used for the reported
experiments is not redistributed with UniFur.
